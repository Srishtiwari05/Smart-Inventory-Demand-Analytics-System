server.port=8080
spring.application.name=smart-inventory-system

# Database configuration with environment variable overrides
db.host=${DB_HOST:localhost}
db.port=${DB_PORT:3306}
db.name=${DB_NAME:smart_inventory}
db.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:smart_inventory}?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
db.username=${DB_USER:root}
db.password=${DB_PASSWORD:Bhanu@2205}

# Python ML Service URL
ml.service.url=${ML_SERVICE_URL:http://localhost:5000/predict/}
